// 配列
A ← {17,32,18,41,52,20,87,12,59,69}
可視化(A)

// 出力
"ソート前 " と A を表示する

// for文
N ← 要素数(A)
i を 0 から N - 1 まで 1 ずつ増やしながら,
    tmp ← i
    j を i + 1 から N まで 1 ずつ増やしながら,
        // if文
        もし A[tmp] > A[j] ならば
            tmp ← j
        を実行する
    を繰り返す
    copy ← A[tmp]
    A[tmp] ← A[i]
    A[i] ← copy
を繰り返す

// 出力
"ソート後 " と A を表示する
